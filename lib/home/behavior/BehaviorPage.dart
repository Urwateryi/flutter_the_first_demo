import 'package:flutter/material.dart';

class BehaviorPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("BehaviorPage"),
      ),
      body: Text("BehaviorPage"),
    );
  }
}
